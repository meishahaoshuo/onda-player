# 2026-09-07 开发日志

## 今日完成：14 项需求改造（会话 44）

### 小修与基础设施
1. **排行榜清空按钮**：移除页面右上角重叠的「清空统计」，移入设置页新增的「数据」分类（含确认弹窗）。
2. **齿轮图标**：settings 图标从滑块样式换成标准齿轮（Feather）。
3. **悬浮按钮融合**：四处（SongList/专辑详情/队列/歌单行）去掉玻璃胶囊底，纯透明图标淡入，与行背景融合。
4. **useDragReorder**：新 composable——Pointer 长按 250ms 触发、拖动项跟手浮动（scale+shadow）、其余项 FLIP 平滑让位、边缘自动滚动、松手回调 onReorder、click 抑制窗口。

### 侧栏
5. **导航重构**：渲染改为 ui.navOrder 驱动（localStorage 持久化），主导航项（含歌单板块整体）长按拖拽排序；歌单子项拖拽排序（playlistStore.reorder，顺序存 kv）。
6. **歌单右键菜单**：新 PlaylistContextMenu + usePlaylistMenu 单例——播放/重命名（内联输入）/设置封面（ui.playlistEditCover 请求标记，PlaylistsView 消费后直接弹封面选择）/删除（内联确认）。
7. **歌单封面 22→28px**，行高与 hover 视觉打磨。

### 新功能
8. **最近在听**：ViewId 'recent' + clock 图标 + RecentView（stats.recentPaths 按 lastPlayedAt 降序，stats store 扩展 lastPlayed 镜像）。
9. **搜索**：顶栏搜索框（聚焦加宽、Esc/清除按钮），输入时内容区渲染 SearchResultsView（标题/艺术家/专辑过滤，SongList 展示，可入队可右键）。
10. **文件夹板块重构**：双栏布局——左列玻璃文件夹卡（权限状态点、悬停浮出恢复权限/移除、选中品牌描边），右列选中内容/引导空态；扫描进度条（带百分比）移入页顶。
11. **添加歌曲弹窗重构**：封面缩略图行、搜索+「仅看未添加」开关、全选/反选/清除、已选计数、选中态高亮。
12. **快捷键系统**：services/hotkeys.ts——默认 Space/Ctrl+←→/Ctrl+↑↓/Ctrl+M，localStorage 持久化，输入框聚焦失效；设置新增「快捷键」分类：点击键帽录制新组合（Esc 取消、冲突覆盖提示、一键恢复默认）。
13. **自定义主题**：主题色板（默认海军蓝+7 预设）+ 自定义取色器；settings.accentColor 派生写 override（--accent-strong 压暗、--accent-text 按亮度对比自动黑/白、--accent-soft/--bg-active/--ambient-fallback color-mix）。**封面氛围光**：内容区背景层跟随当前播放封面取色双光斑呼吸（Transition 淡入淡出），设置开关控制。
14. **歌单封面过渡**：PlaylistsView 重构为「网格常驻（v-show）+ 详情覆盖层」，点卡片走引力坍缩编排器（beginAlbumEnter 泛化 gridSel/cardSel/wave 选择器，歌单用 .pl-grid/.pl-card/.pl-info/.drag-row），返回 playAlbumExit 对称；删除 legacyFlip.ts。
15. **歌单拖拽丝滑化**：原生 HTML5 DnD（插入指示线）换成 useDragReorder（长按+跟手+FLIP 让位）。

## 遇到的问题
- 同元素同属性挂两个 delay 动画：后建动画的 backwards 填充在延迟期压过先建动画（上一会话教训，本次编排直接采用单一 offset 时间线规避）。
- 「最近在听」验证需 lastPlayedAt 数据：注入 IndexedDB stats 后 reload，验后清理。
- 后台页 IntersectionObserver 回调不触发导致 stagger 元素保持 opacity 0（截图白屏假象），前台正常。

## 待办 / 用户实测
- 侧栏拖拽排序、歌单行拖拽 FLIP 的真机手感（自动化环境后台页无帧，pointer 拖拽无法实测）。
- 快捷键 Space 实际触发（环境无法真实播放）。
- 氛围光在深色主题下的观感、自定义取色器的实际使用体验。

## 追加：拖拽体验修复（用户反馈卡顿/割裂/点击不流畅）
用户报告拖拽有 bug 且影响正常点击。修复：
1. **拖拽态残留（真 bug）**：原实现没监听 pointercancel/窗口失焦，拖拽中断后拖拽态永久卡死；且 cursor:grabbing 与 zIndex 只设不清。重写退出路径：pointerup/pointercancel/blur 统一走 reset()，完整清理 cursor/zIndex/willChange/让位 transform，并在真正发生过拖拽时才设 200ms click 抑制窗。
2. **阴影割裂**：拖拽项去掉投影（shadow-2 与整体风格不符），改为背景提亮 + 1.015 轻缩放。
3. **卡顿**：pointermove 同步重排改 rAF 节流（每帧最多一次位移计算）。
4. **歌单板块位置**：从主导航拖拽组移回分隔线下方固定（不再参与板块排序），仅主导航项与歌单子项各自内部可拖拽——正是需求范围；navOrder 默认表与存储过滤同步剔除 playlists。
5. 验证注意：同步读 h1 会因 Vue 异步渲染误判点击失效，需 sleep 后读取；实测专辑→最近在听→歌曲连续导航均正常。
6. 拖拽项底色与选中高亮重叠：拖拽项不再附加背景色块，保留更长的那块（侧栏=选中胶囊、歌单行=播放行高亮），反馈仅靠轻缩放与抓取光标。

## 追加 8：九项反馈改造（会话 45）

### Bug 修复
1. **歌词页切歌穿模**：coverFlight 的「机上换带」watcher 不检查歌词页状态，卡带挂 body（z70）浮在歌词页（z50）之上。`installTrackSwapWatcher` 增加 `ui.lyricsOpen` 守卫——歌词页开着时跳过卡带动画（歌词页有自己的切歌过渡）。
2. **侧栏点击热区只有文字（根因实锤）**：`<button>` 即使 `display:flex` 也不会像块级元素那样撑满父容器（width:auto 是收缩宽度），`.nav-item` 实测只有 96px 宽——悬停灰底只盖住文字一截，选中胶囊却是整行（用户描述的"两个阴影冲突"+"空白点不到"）。修复：`.nav-item { width: 100% }`；同时选中项悬停不再叠加灰底（`.nav-item.active:hover { background: transparent }`），胶囊成为唯一强调层。
3. **长按吞点击**：useDragReorder 按住 ≥250ms 即进入拖拽态，松手一律抑制 click 200ms——刻意稍慢的点击会被吞。改为只有真的拖拽位移过（dragged 标记）才抑制，纯长按未动松手放行 click。合成 pointer 事件实测：按住 300ms 松手后导航正常。
4. **歌单返回动画结尾不完整**：网格 `v-show="!current"` 要到 playAlbumExit 末尾才显示，"卡片喷回"收尾动画跑在 display:none 容器上。去掉 v-show 让网格永久渲染（详情覆盖层不透明底盖住，与专辑详情同构），另加 closing 防重入守卫。实测返回中网格可见、终态完整。

### 功能与视觉
5. **排行榜表头**：从页面顶部移到第 4 名起的列表区上方（领奖台区不带表头），sticky 跟随滚动，与行列同 grid 严格对齐（实测 Δx=0）。
6. **添加歌曲大号二级弹层**：520px 小弹窗 → 900×640 大弹层；标题行（歌单名副标题 + 关闭钮）→ 搜索栏 + AppSwitch「仅看未添加」→ 全选/反选/清除 → 弹性撑满的滚动列表（44px 封面、双行信息、时长列、已在歌单灰显）→ 底部固定操作条。
7. **侧栏歌单封面 28→36px**，子项行高 40→48px，圆角 8px。
8. **AppSwitch 共享开关**：新组件（spring 滑块 + accent 实底 + 按下缩放），设置页「封面氛围光」与添加歌曲弹层统一换用，删除两处各自为政的 toggle 样式。
9. **歌单自定义排序**：移除歌单详情内拖拽（侧栏拖拽保留），列表工具栏加排序下拉——自定义顺序/歌曲名/艺术家/专辑/播放次数/时长，选择持久化 kv（`playlists.songSort`）；显示顺序与播放队列分离。实测切换与刷新持久化通过。
10. **歌单详情头部对齐专辑详情页**：渐变底 + 双呼吸流光光斑（取色自当前封面，带缓存）+ 胶片噪点，封面 120→176px，统计行（N 首歌曲 · 总时长），操作按钮沿用专辑页规格。

### 验证与环境备注
- 浏览器逐项验证（真实曲库 37 首）：排行榜表头、添加弹层多选添加、排序切换+刷新持久化、返回动画终态、侧栏全行热区与长按点击、设置/弹层开关。
- 环境坑：IAB 标签页 hidden 时 rAF 停摆 → Vue out-in 过渡卡在 leave-from、截图超时；用手动派发 transitionend「泵」过渡继续验证。真机无此问题。
- 验证数据已清理（注入的 8 条统计、songSort kv、临时测试歌单）。

## 追加 9：用户回访两修复（会话 45 续）
1. **歌单详情头部被压扁**：`.playlist-detail` 是固定高度纵向 flex 滚动容器，头部新加的 `overflow:hidden` 使其 min-height 归零，内容超高（21 首）时整个头部被 flex 压缩成一条（专辑详情页同款坑，当年靠 `.album-detail > * { flex-shrink: 0 }` 修复）。补 `.playlist-detail > * { flex-shrink: 0 }`。注入 30 首实测：头部保持 232px、封面完整。
2. **侧栏胶囊大跨度过冲越界**：滑动胶囊全程弹簧缓动（过冲系数 1.56），大跨度（如设置→歌曲）时过冲冲出导航区。改为动态缓动：位移 >100px 判为远跳（`.nav-pill.far`），切无过冲 ease-out；近距离保留弹簧手感。隐藏页签下验证了 far 类随大跨度同步生效（CSS 规范保证新 transition 值作用于新启动的过渡），真机手感待用户确认。
- 环境备注：隐藏页签 setTimeout 亦被节流，采样脚本读到的过渡滞后一个窗口，验证以 class 状态 + CSS 语义为准。

## 追加 10：歌单间切换过渡（会话 45 续 2）
用户反馈在自建歌单之间直接切换时无过渡、很生硬。根因：侧栏点歌单只改 detailKey，详情覆盖层内容瞬间替换；编排器的无起点路径只做 revealed 兜底、无可见动画。
方案：详情内容（头部+工具栏+列表）套 `<Transition name="view" mode="out-in">`，以 `current.id` 为 key——歌单间切换复用与顶部板块切换完全同款的淡出→上浮淡入动向；打开/关闭仍由外层 v-if 走引力坍缩编排（初始挂载不播 enter，无双重动画）。切换时详情滚动归零；编排器 watcher 改为仅从无详情进入时跑 playAlbumEnter。
验证：切换时 leave/enter 过渡类均出现，切换后内容/滚动正确。真机观感待用户确认。

## 追加 11：返回按钮改隐藏式（会话 45 续 3）
用户反馈「返回歌单列表」独占一行太碍事。移除该行，改为详情头部渐变面板右上角的隐藏式圆形关闭钮：默认 opacity 0 + 上移 4px，悬停头部卡片或按钮聚焦时浮现（过渡浮现 + 轻微下滑入位），悬停按钮本体时底色加深；title/aria 均为「返回歌单列表」。已验证默认隐藏、定位在角内、点击正常返回网格。

## 追加 12：专辑/艺术家详情返回改隐藏式（会话 45 续 4）
与歌单详情同款处理：移除左上角独占一行的「返回专辑/艺术家列表」，改为头部右上角隐藏式圆形关闭钮（默认透明上移，悬停头部或聚焦时浮现）。专辑页锚在渐变面板内 12px（z-index 3 盖过光斑与噪点）；艺术家页头部无面板底，锚在头部行内 4px（避免被 .artist-detail 的 overflow:hidden 裁切）。两者的波前入场编排（.revealed gating）不再包含返回按钮——按钮有自己的悬停浮现逻辑。已验证：默认隐藏、定位正确、点击走原编排关闭。

## 追加 13：歌词页五项调整（会话 45 续 5）
1. **去除歌词偏移**：删除 settings 的 lyricOffset/setLyricOffset/nudgeLyricOffset 与歌词页的偏移 UI、时间轴平移逻辑（groups 不再整体偏移）。
2. **字号/字重无级 + 对齐切换**：4 档字号改为 14-44px 连续滑杆（翻译行按 0.66 比例派生 lyricSubPx）；新增字重 300-800 无级滑杆（活动行 = 基础字重 +200）；新增居中/左对齐分段选择（text-align 走 --lyric-align 变量）。旧档位类型 LYRIC_FS_STEPS/LyricFontSize/cycleLyricFontSize 全部移除。
3. **歌词景深模糊**：非当前行按距离 0.4→2.6px 模糊（dim-1~4），filter 不参与 transition（多行过渡掉帧），突变由透明度层次掩盖；设置面板可开关（默认开）。
4. **背景模糊过渡**：环境背景改双层结构——切歌时新层以 opacity 0 挂载、双 rAF 后 600ms 淡入、900ms 后旧层退役，实现交叉淡化；首层仍与封面飞入并行 reveal。色彩策略（预烘焙 ambientUrl + 明亮色提取）不变。
5. **控制组件风格切换**：新增「稳重 / 简约」分段设置——简约风为 Apple Music 式小尺寸（播放键 48px、图标缩小一档、悬停无底色放大），字号滑杆同面板内切换即时生效。设置菜单重做为 236px 面板：两根无级滑杆 + 两组分段 + AppSwitch 模糊开关。
验证（隐藏页签 + rAF 垫片）：偏移 UI 已无、字号/字重变量即时生效、对齐切换生效、模糊类切换生效、简约控制条 48px 生效、切歌时背景双层共存后归一。

## 追加 14：背景过渡回退 + seek 定位慢修复 + 玻璃拟态控制风格（会话 45 续 6）
1. **背景过渡动画回退**：用户去掉交叉淡化——恢复单层环境光，切歌直接换图（ambientUrl/ambientShown 原样，双层结构删除）。
2. **歌词 seek 定位慢修复（三个叠加 bug）**：
   a. `scrollToActive(false)` 直接赋值 scrollTop 触发的 scroll 事件无守卫（守卫只看动画期 scrollRaf），被误判为用户滚动 → 自动跟随冻结 3 秒；改为程序化滚动打 120ms 宽限时间戳，scroll 事件先查时间戳。
   b. `smoothScrollTo` 完成后 scrollRaf 从不清零，陈旧真值让守卫永久失效（用户滚动检测失灵）；完成分支归零。
   c. `watch(activeIdx)` 的 userScrolling 一票否决把 seek 也吞了——大跨度跳行是明确意图，改为冻结只拦 ±1 行的顺次换行，seek 立即定位。
   另：`player.seek` 同步刷新 `currentTime.value`（原先等 timeupdate，最多差几百毫秒），歌词高亮/进度条即时重算。
3. **第三种控制风格「玻璃拟态」**：settings 增 glass——控件磨砂玻璃圆底（blur+描边+内高光），播放键 56px 品牌色实底+品牌色投影；设置菜单分段扩为 稳重/简约/玻璃。
验证：背景单层直换、seek 同步刷新 currentTime（0→120 即时）、玻璃风格 56px 播放键与品牌色背景（隐藏页签动画时间线冻结导致过渡色卡在起始值的假象已用去过渡复验排除）。

## 追加 15：歌词定位再提速（会话 45 续 7）
滚动动画从固定 420ms 改为按距离缩放（140ms 起步、每 12px 加 1ms、上限 300ms），超过 2400px（约两屏，典型为 seek 跨段）直接定位不跑动画——seek 后歌词几乎即时落位，顺次换行也更跟手。

## 追加 16：移除控制样式选项（会话 45 续 8）
按用户要求去除「控制样式」设置（稳重/简约/玻璃三选），控制器固定为稳重风格。删除 settings 的 lyricControls/LyricControlsStyle/setLyricControls、歌词页的菜单分段、minimal/glass 类绑定与两套 CSS、ctrlIcon 动态尺寸（恢复固定 20/24/28）。对齐分段控件保留（仍被居中/左对齐使用）。

## 追加 17：品牌更名 Onda Player（会话 45 续 9）
播放器名从「ONDA · 澜」改为「Onda Player」：页面标题、PWA manifest（name/short_name）、侧栏品牌区（去掉「澜」副标及样式）、设置页关于区（描述/alt/名称）、favicon 与 logo SVG 的 aria-label、main.css 注释统一替换。

## 追加 18：彻底移除自定义壁纸功能（会话 48）
会话 47 壁纸方案定稿后，第 6 轮通透度优化（模糊派生 + 遮罩再平衡 + 控件加固）已构建通过但未提交，用户改主意要求彻底去除——全部优化转为拆除对象：
- 删 `services/wallpaper.ts` 整文件；App.vue 壁纸层模板/样式/`initWallpaper()` 调用（替换为启动时清理 IndexedDB 遗留 `wallpaper`/`wallpaper-blur` kv，静默 catch）
- SettingsView 壁纸选择/清除/通透程度滑杆 UI 与 `.wp-*` 样式（保留封面氛围光）
- settings store：浓度 key/loader/ref/`--wallpaper-intensity` watchEffect/setter/导出
- main.css：两主题 `--wallpaper-veil*` 令牌 + 整个「壁纸模式」区块（详情层 attachment:fixed 透壁纸、`data-wallpaper` 玻璃加固、控件半透明规则）
- ChartsView 关联注释；cover.ts `downscale` 还原模块私有
验证 verify-wp-rm.mjs 13/13 PASS 零异常：seed 伪造遗留 kv → 重载被启动清理清空；无 .wallpaper-layer/data-wallpaper/变量；详情层背景回落 none+scroll；设置页无壁纸文案且氛围光保留；dark/light 各视图正常。踩坑：排行榜 `.list-header` 只在第 4 名起渲染，冒烟 seed 必须写 putStat 播放次数；ChartsView 在 views/ 不在 components/。

## 追加 19：文件夹页空态布局修复（会话 48）
**根因**：`.primary-btn/.ghost-btn` 共享规则里的 `flex:1`（左栏底部工具栏横向均分用）落到空态纵向 flex 容器后作用到主轴，把「添加文件夹」按钮拉成通屏高竖条（用户截图实证）。
**修法**：`flex:1` 收进 `.side-toolbar` 作用域根因修复；空态 48px 裸图标升级为 76px 玻璃圆角底座（--glass-* 令牌，与 root-card 玻璃语言呼应）+ 间距文案层级微调。
验证 verify-folders-empty.mjs 5/5 PASS：空态按钮 115×33 正常胶囊、底座 76×76 近方形；注入文件夹后工具栏两按钮 129/131px 均分（回归通过）；深浅两主题截图确认。

## 追加 20：歌词页「封面色场」改造（会话 48，对照 Salt Player）
用户提供 Salt Player 歌词页截图，要求学习其背景色彩模糊逻辑。用户确认三取向：①高亮柔和（深色文字）②文字/控件跟随封面主色 ③切歌 260ms 短淡入（不做双层交叉淡化）。

### 实现
- **palette.ts 新增三个导出**（现有 renderAmbientUrl/makeAmbientGradient/extractBrightColors 保留，专辑详情与 App 氛围光仍用）：
  - `buildCoverField(blob)`：封面 cover-fit 缩放（过扫描 1.12）→ 画布内 `blur(20px) saturate(0.7)` → 白色半透明自适应提亮（`whiteAlpha=clamp(0.4+(1-lum/255)*0.34, 0.4, 0.72)`，暗封面多提亮）→ 输出 256×(144~256)（按视口宽高比）PNG dataURL；**返回前从成品画布采样平均色 bgEff**（base 是压暗色，不能估算显示亮度）；模糊全部预烘焙，无实时全屏 CSS blur（项目铁律）
  - `pickMainColor(foci)`：主色取「饱和度最高 + 亮度适中」的分块（`score=sat*(1-|lum-140|/200)`），不用 4×3 均值（会被平均成浊灰）；灰阶封面兜底暖深灰
  - `deriveLyricVars(main, bgEff)`：WCAG 对比度循环压暗（保色相），当前行/标题/控件 ≥7:1、其余行 ≥4.5:1，派生 21 个 `--lyric-*` 动态令牌
- **LyricsFullView.vue**：
  - `fieldCache: Map<coverId,{url,vars}>` 替换 ambientCache+flowCache；watch(coverId) 烘焙+派生+缓存
  - `lyricVars` computed 展开 coverPalette → 内联到 `.lyrics-full` 根，子元素自动继承（卡拉 OK 渐变/mask/进度条/控件全在后代）
  - `.bg-ambient.show` opacity 0.16→**1**（从淡淡点缀变整页色场）、transition 280→260ms、呼吸收敛 scale1.04/14s
  - **删除 .flow 流光圆斑**（色场自带颜色，冗余且省两个合成层）
  - 修掉 `.bg` 渐变硬编码 `#fbf9f4`（改 color-mix，守「颜色只引用变量」约束）
  - reduced-motion：`.bg-ambient` animation/transition none
  - 无封面/取色失败 → coverPalette 为空对象 → 回落 main.css 静态米白（保持不动作兜底）

### 验证（verify-coverfield.mjs，15/15 PASS 零异常）
背景为烘焙 dataURL、opacity=1、单层无交叉淡化、无 .flow 残留、mask 不透明；底色亮度 0.701（浅场）、当前行深色 0.056、对比度 7.11:1（当前行）/ 5.00:1（其余行）/ 7.11:1（控件）；无封面歌回落 #1d1d1f/#f6f3ec；reduced-motion 过渡 0s、动画 none。截图：紫封面→粉紫色场+深紫文字，无封面→米白兜底。
**踩坑**：SongList 行是**单击**播放（@click onRowClick），dblclick 不触发——验证脚本先点错且 addCoverSong 的歌按标题排序在**第一位**（C<T），两个因素叠加导致首轮假失败。

## 追加 21：立体歌词（景深阶梯增强，会话 48，对照 Salt Player 截图）
用户再发 Salt Player 歌词页截图，要求「立体歌词效果」。现状 dim 行仅 scale 0.97↔1 + 最大 2.6px 弱模糊，与 Salt 差距核心是**大小差几乎为零 + 模糊不够深**。
改造（仅 LyricsFullView.vue 样式段）：
- 当前行 `scale(1.08)`（原 1）——Salt 式放大强调
- 远行缩小阶梯：dim-1 0.96 / dim-2 0.93 / dim-3 0.91 / dim-4 0.89（基线 0.9）
- 模糊陡增：0.8 / 1.8 / 3.4 / 5.5px（原 0.4/1/1.8/2.6，最远行几乎融进背景）
- 透明度加深：0.55 / 0.4 / 0.28 / 0.18
- 策略不变：只动 opacity/transform 参与 transition，blur 突变被透明度掩盖（多行 blur 过渡掉帧的既有结论）
验证 verify-depth.mjs 6/6 PASS：active scale 1.078/op 0.997，dim-1 scale 0.959/op 0.55/blur 0.8px 全中期望值。测试歌仅 2 行 LRC，dim-2~4 与 dim-1 同构由 CSS 保证。
**验证环境新坑**：headless 需 `--autoplay-policy=no-user-gesture-required`，否则合成 click 播放被自动播放策略拦截，`player.playing` 恒 false、active 行永不出现（表现为「样式全对但类没挂上」）。
