# 2026-09-12 开发日志

## 1. 顶栏磨砂玻璃化 + 移除列名表头（会话 52；一日三迭代：液态玻璃表头 → 断层 → 去表头 → 顶栏改磨砂）

**迭代轨迹**（用户边看 HMR 实时效果边反馈）：

1. 先按要求给各页列表表头加「液态玻璃」（SongList + ChartsView 的 `.list-header`，新增 main.css `.liquid-glass` 工具类，白纱 + backdrop 折射，替换原 `var(--bg-base)` 不透明底）。
2. 用户截图反馈「上面部分也做液态玻璃，现在断层了，应该是一个整体」——顶栏（`view-header`：页面标题 + 搜索框）还是不透明的。改为**整体方案**：
   - `view-header` 提出文档流变**悬浮层**（absolute + z-index 3，低于详情覆盖层 z5），高度收敛为 `.content` 上的 `--head-h: 68px`（padding 20/12 + 36px 内容）；
   - `.view-body` 顶部 padding 改 `var(--head-h)`，内容首屏从顶栏下缘开始、滚动后从顶栏玻璃底下穿过；
   - 吸附类元素 top 统一走 `--list-sticky-top`（view-body 内 = 顶栏高度；详情覆盖层/文件夹右列等独立宿主默认 0，FoldersView 曾加覆盖后随表头移除一并删掉）；设置页左列导航同步改用该变量。
3. 用户随即改主意：「直接去掉标题艺术家这个列表表头吧」——SongList（歌曲/我喜欢的/专辑与艺术家详情/搜索/文件夹右列共用）与 ChartsView 的列名表头整个删除，行自身 grid 对齐不变；顺带清理全部相关样式与过时注释（含 FoldersView 的 sticky 覆盖）。
4. 用户再定材质：「改为普通磨砂效果吧」——顶栏弃液态玻璃改**普通磨砂**：`var(--glass-bg)` + `var(--glass-blur)` + `border-bottom: var(--glass-border)`（与队列面板/玻璃卡同语言）；`.liquid-glass` 工具类无使用处，从 main.css 移除。

**最终形态**：顶栏 = 悬浮普通磨砂条（内容从其下穿过）；列表无列名表头，行直接从顶栏磨砂底下滚过；设置页导航仍吸在顶栏下缘（`--list-sticky-top` 机制保留）。

**验证**：`npm run build`（vue-tsc）通过；观感由用户在 dev server（HMR 实时）直接确认。

## 遇到的问题

1. **表头液态玻璃必然带出「断层」**：滚动宿主（view-body）在顶栏下方，行只能在被表头覆盖的区段里露出玻璃质感，顶栏是页面底色——材质在两者交界处硬切。解法不是给顶栏也涂同一材质（顶栏 backdrop 里没有行，观感仍对不上），而是把顶栏变成悬浮层、让行物理上从它底下穿过。
2. **悬浮顶栏吃掉 sticky 基准**：view-body 顶边到了页面 y=0，`top: 0` 的吸附元素会钻进顶栏背后。引入 `--list-sticky-top`（.content 定义 `--head-h`，view-body 换算），各吸附位一处变量全局联动。
3. **ChartsView 去表头时手滑产出重复 `.chart-row` 规则**，下一笔编辑立即收成单条。

## 遗留

- 歌曲列表/排行榜删了列名后，「专辑/艺术家」列的语义只靠行内容自明；若后续要恢复轻量列名（如滚动后才浮现的幽灵表头），可基于 `--list-sticky-top` 机制加回。
- 会话 51 的六项改动已在 `ae612d0` 提交；本会话改动待本次提交。

## 2. 补记：「改为普通磨砂」后顶栏仍完全透明的排查（环境坑）

用户反馈顶栏完全透明、没有磨砂。无头 Chrome 读计算样式：`position/z-index` 是新的、`background/backdrop-filter` 声明**根本不在样式表里**——Vite 的文件监听漏掉了最后一次 App.vue 编辑，样式子模块的 `?t=` 时间戳没变，dev server 一直在给浏览器发旧 CSS（`npm run build` 是独立进程所以是通过的，掩盖了这一点）。`touch src/App.vue src/styles/main.css` 强制失效后，计算样式立即变为 `var(--glass-bg)` 渐变 + `blur(32px) saturate(1.4)`，截图确认磨砂生效（`devlog/.shots-2026-09-12/header-frosted-light.png`）。以后凡是「改了没生效」先怀疑 HMR 停摆：`npm run build` + 无头读计算样式双交叉，再动代码。
