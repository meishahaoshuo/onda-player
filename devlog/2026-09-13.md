# 2026-09-13 开发日志

## 今日完成
- 开启并完成第 8 阶段「桌面化」【第 8 阶段】：
  - 新增 `public/sw.js`（手写，无 workbox 依赖）：预缓存应用外壳（`/`、manifest、favicon、192/512 图标），同源哈希静态资源 cache-first，导航 network-first + 离线回退外壳；明确不缓存音频/歌词内容（blob URL 本就不经 SW）；activate 清理旧版本缓存；仅生产注册（`main.ts` PROD 分支）
  - 新增 `src/services/pwa.ts`：`beforeinstallprompt` 捕获（prompt 一次有效用后即弃）、`display-mode: standalone` 检测（含 iOS `navigator.standalone`）、`appinstalled` 监听、`promptInstall()` 安装流程
  - PWA 图标补齐：PowerShell System.Drawing 由 1200px logo 生成 `onda-icon-192/512.png`；manifest 补 192/512 图标、`id`、`launch_handler: focus-existing`（已运行则聚焦不重复开窗）、`short_name`
  - 设置页「关于」新增「桌面应用」区块：可安装（主题色按钮）/ 已在独立窗口运行 / 不支持说明三态文案；`icons.ts` 补 download 图标
  - `server.mjs` 补 `.webmanifest` → `application/manifest+json` MIME
  - 文档：02 技术栈/目录/已知限制更新，04 新增第 8 阶段

## 遇到的问题
- 自动化内嵌浏览器窗格被遮挡时 Chromium 节流 rAF，Vue 过渡（view/panel 的 nextFrame）停在 leave-from 不再推进，页面看似"卡死"。强制窗格可见后过渡立即恢复——判定为自动化环境渲染节流，与应用无关；重载页面后同样操作即正常
- 内嵌浏览器不派发 `beforeinstallprompt`，安装按钮链路用合成事件（带 prompt/userChoice 的伪 Event）验证通过

## 验收记录
- dist + server.mjs（5181）：SW activated、scope `/`；`onda-shell-v1` 缓存恰为 5 项外壳资源且 `/` 可命中；manifest 正确 MIME + 3 图标 200
- 模拟安装：派发事件→按钮出现→点击→accepted→按钮消失、文案切到"已安装"
- `npm run build`（vue-tsc）通过；设置页「关于」截图确认视觉

## 明日待办
- 用户真机实测：真实 Chrome 安装弹窗、桌面快捷方式、独立窗口观感、断网离线加载（已记入 04 用户实测清单）

---

# 会话 2：Tauri PoC 与第 9 阶段立项【第 9 阶段】

## 今日完成
- **Tauri 2 PoC**（背景：用户决定做原生桌面端，候选 Tauri 2 vs Electron，事先约定「FSA 不可用即切换」的判定标准）：
  - 环境就绪（前一会话）：MSVC 生成工具（D 盘最小组件集）+ Rust 1.98.1（rustup 静默安装，`RUSTUP_HOME`/`CARGO_HOME` 指 D 盘，rsproxy 镜像），冒烟测试 `cargo build` 通过
  - `npm i -D @tauri-apps/cli`（2.11.4）+ `tauri init` 生成 `src-tauri/`；`tauri.conf.json` 定 productName "Onda Player"、identifier `com.ondaplayer.app`、devUrl 5180、窗口 1280×800、NSIS；`package.json` 增 `desktop: tauri dev`
  - 首次 cargo 全量编译（438 crates，rsproxy 镜像）顺利，桌面窗口拉起；`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9223` 开调试口，复用仓库纯 Node WS 的 CDP 模式连入实测（脚本 `devlog/.shots-2026-09-13/poc-webview2.mjs`）
- **PoC 结论**：backdrop-filter ✅ / FLAC、OPUS "probably" ✅ / MediaSession 存在 ✅ / **`showDirectoryPicker` = undefined ❌**（WebView2 有意禁用整套 FSA，微软文档明示）
- **路线定案（用户拍板）**：维持 Tauri 2，文件接入层原生化改造（不再宣称前端零改动）——tauri-plugin-dialog 选目录、Rust 枚举 path/size/mtime、自定义 `media://` 协议 Range 流式播放、parseBuffer 解析、IndexedDB 存目录路径取代句柄（顺带收益：桌面端不再有每次启动的授权确认）；平台差异收敛在 fs/scanner/player 服务层
- 文档：docs/02 技术栈表加 Tauri 2 行 + 新增「桌面端重要说明」+ 目录结构补 src-tauri；docs/04 新增第 9 阶段（PWA 已占第 8）并标进行中

## 遇到的问题
- WebView2 无 FSA（核心问题，见上）→ 触发预案；用户选择改造而非换壳，方案改为原生文件接入
- 5180 端口已被用户开着的 Vite 占用（strictPort 报错）→ 直接复用现有 dev server，不另起
- Electron 二进制下载失败（直连 GitHub 被网络挡，ELECTRON_MIRROR 未生效）→ 用户取消重试；electron 仍在 devDependencies（用户取消了 `npm uninstall electron`），**遗留待确认清理**

## 明日待办
- 9.1 文件接入层改造（Rust 侧 media:// 协议 + 目录枚举命令；fs/scanner/player 平台分支）
- 确认后清理 devDependencies 里的 electron 遗留

---

# 会话 3：9.1 文件接入层改造完成 + PoC 结论修正【第 9 阶段】

## 今日完成
- **Rust 侧**（src-tauri/src/lib.rs，约 230 行）：
  - `media://` 自定义协议：Range 解析（a-b / a- / -n 三形态）、4MB 分块封顶流式返回、Content-Range/Accept-Ranges、按扩展名 MIME、CORS `*`——audio.src 直接指向协议 URL，整文件不进内存
  - `list_audio_files`：递归枚举（跳 `.` 隐藏目录），返回 {abs, rel（`/` 分隔）, size, mtime_ms}
  - `read_head`：头部 2MB 字节走 `tauri::ipc::Response` **二进制通道**（普通 Vec<u8> 会序列化成 JSON 数字数组，MB 级不可接受）
  - `read_text_file`：.lrc 读取；dialog 插件注册 + capability `dialog:default`
- **前端平台分支**（差异收敛在服务层）：`types.RootRef`（FSA 句柄 | `{desktopPath}`）；fs.ts 重写（pickFolder 原生对话框、enumerateAudioFiles 双形态、songMediaUrl、readHeadBytes、readLrcFile、权限 desktop 恒 granted）；scanner `parseBuffer(head, {size}, {duration:true})`；player 服务 `loadSource(File|string)`、store load 分支；cover.ts 高清封面分支；library store 类型适配
- **vite.config.ts**：watch ignored 排除 `src-tauri/target`——cargo 改写 DLL 会让 chokidar EBUSY 崩掉 dev server（用户原 5180 服务器即此故身亡，这也曾让页面停在错误页）
- **CDP 实测全绿**（脚本 poc-desktop-91.mjs，桌面窗口 WebView2 内）：枚举含子目录 ✅ / read_head ArrayBuffer 2MB RIFF ✅ / media:// 206 流式 ✅ / Audio 真实播放 30s、currentTime 2.47s ✅ / **seek 跳 20s 续播 21.46s ✅** / .lrc 中文 ✅ / localStorage+IndexedDB ✅

## 遇到的问题（重要）
- **PoC 结论修正**：首轮「showDirectoryPicker undefined → FSA 被禁」是**假象**——真实原因是 WebView2 停在 `chrome-error://chromewebdata` 错误页（dev server 挂了 + 系统代理两重因素），错误页上 FSA 整套不可见。页面真实加载后 FSA 完整存在且手势机制正常（无手势调用报标准 SecurityError）。已回写 docs/04；桌面主链路仍走原生化路径（不赌 WebView2 的权限弹窗/句柄持久化行为），FSA 留作后备
- tauri dev 重编译报「拒绝访问 app.exe」→ 残留进程 taskkill 后恢复
- IPC 报「Origin header is not a valid URL」→ 根因同上（错误页 Origin 为 null），页面正常后消失
- dev 启动链固定为：先 `npm run dev`（5180）再 `npm run desktop`（`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` 加 `--remote-debugging-port=9223 --no-proxy-server`）

## 明日待办
- 9.2：窗口 backgroundColor 防闪白；用户真机走一遍 选目录→扫库→播放→歌词
- 9.3 默认配置烘焙；electron 遗留依赖清理待用户确认

---

# 会话 4：9.2a 收尾 + electron 清理【第 9 阶段】

## 今日完成
- electron 遗留依赖卸载（用户确认）；package.json 已无 electron
- `tauri.conf.json` 窗口 `backgroundColor: #0A0A0A` 防启动白闪（静态深色；随主题动态背景色留到 9.5 与磨砂一起做）；tauri dev 检测到配置变更自动热重启
- 桌面窗口启动回归（CDP）：sidebar 渲染 ✅ 品牌 "Onda Player" ✅ 主题解析 light ✅ 控制台 0 报错 ✅

## 遇到的问题
- 无（backgroundColor 为 v2 窗口配置原生字段，一次通过）

## 明日待办
- 用户真机确认 9.2b：添加文件夹（D:\onda-test-music）→扫库→播放→拖进度→歌词
- 9.3 默认配置烘焙（需要用户提供浏览器端当前设置值）或 9.4 打第一版 NSIS 安装包

---

# 会话 5：9.3 默认配置烘焙【第 9 阶段】

## 今日完成
- 直接从用户 Chrome 的 localStorage leveldb 读取配置（复制 .ldb/.log 到临时目录后二进制解析，绕开运行锁；UTF-8 键 + latin1 值 + accentColor 取追加日志最后一条历史记录）
- 配置快照：themeMode=system / accentColor=#EC4141 / playerStyle=capsule / barMaterial=liquid / barMorph=gather / locateFabStyle=compass / lyricAlign=left / lyricFontSize=41 / lyricFontWeight=800 / lyricBlur=1
- settings store 六处出厂默认改写（字号 22→41、字重 500→800、对齐 center→left、播放条 standard→capsule、accent ''→#EC4141；「默认海军蓝」色板仍显式回退 ''）
- `npm run build` 类型检查通过；桌面窗口重载验证：`--accent`=#EC4141、`--accent-text` 白、胶囊播放条渲染、localStorage 零写入（出厂态即作者观感）

## 遇到的问题
- leveldb 值解析：accentColor 在 .ldb（旧压实层）与 .log（追加层）值不同，取 .log 最后一次写入为准
- Git Bash 的 /tmp 与 Node 路径解析不一致（node 把 /tmp 解析到 D:\tmp）→ 用 cygpath -m 转换

## 明日待办
- 9.4 NSIS 安装包（ONDA logo 生成图标 + tauri build + 真机安装卸载）

---

# 会话 6：9.4 第一版 NSIS 安装包【第 9 阶段】

## 今日完成
- 图标：`npx tauri icon public/logo/onda-logo-app.png`——选白底圆角海军蓝 logo（app 版）而非透明底白色声波（main 版在浅色任务栏/资源管理器上不可见）；全套 ico/png/icns/安卓 mipmap 生成
- `tauri.conf.json` 补 `beforeBuildCommand: npm run build`（此前为空，打包会用旧 dist）
- `npx tauri build`：release 全量编译 4m52s（rsproxy 顺利），NSIS 3.11 与 nsis_tauri_utils 从 GitHub 自动下载成功
- **产物**：`src-tauri/target/release/bundle/nsis/Onda Player_0.1.0_x64-setup.exe`（26 MB；未压缩主程序 43 MB），含烘焙默认配置与原生化文件接入

## 遇到的问题
- `npm run tauri build` 不存在（无该 script）→ 改 `npx tauri build`；顺手起名为「desktop」dev 用 script，build 直接走 npx 即可

## 明日待办
- 用户真机：双击安装包安装 → 启动 → 添加文件夹 → 播放 → 卸载验证
- 9.5 桌面特性：无边框自绘标题栏（用户已确认去掉系统灰条）+ 窗口磨砂 + 托盘 + 单实例

---

# 会话 7：修复主题色烘焙回归 bug【第 9 阶段】

## 今日完成
- 用户反馈：默认主题色是 ONDA 蓝，启动却是网易云红，改回蓝后重启又变红
- 根因两个：① 快照里的 #EC4141 是色板「网易云红」的试色残留，非用户真实默认（用户口径＝海军蓝）② 初版烘焙把 loadAccent 的兜底写成 '#EC4141'，而「默认海军蓝」的存储语义是空字符串——'' 经「无效值→兜底」路径重启后被强行兜回红色
- 修复：loadAccent 兜底改回 ''（海军蓝），并留注释禁止再用「无效值→非空出厂色」写法；出厂默认即海军蓝
- 重新 tauri build（增量，仅前端 + NSIS 重打包），安装包已更新

## 遇到的问题
- 教训：烘焙默认值时，凡是「哨兵值语义」的配置（'' 有含义），兜底必须与哨兵语义一致，不能想当然换成快照值

## 明日待办
- 9.5 桌面特性：无边框自绘标题栏 + 磨砂 + 托盘 + 单实例

---

# 会话 8：9.5 桌面特性全量 + 0.2.0 安装包【第 9 阶段】

## 今日完成
- **无边框自绘标题栏**：`decorations:false + transparent:true`；TitleBar.vue（36px 拖拽区 + 最小化/最大化[图标随状态]/关闭三按钮，关闭 hover 红 #e81123，样式走主题令牌）；关闭按钮触发 CloseRequested → Rust 隐藏到托盘
- **窗口磨砂**：`set_window_effect(dark)` 命令 + window-vibrancy Acrylic（深 tint (14,14,18,125) / 浅 (242,243,245,150)，先 clear 再 apply）；desktop.ts 加 `html.desktop-glass` + MutationObserver 监听 data-theme 重调；main.css 基底 `color-mix(--bg-base 76%, transparent)`；系统不支持静默降级
- **托盘**：tray-icon 特性，菜单（显示主窗口/播放暂停/上一曲/下一曲/退出）+ 左键单击唤窗；播放控制 `tray://playpause|prev|next` emit → desktop.ts 转 DOM 事件 → App.vue 调 player store
- **关闭=最小化到托盘**（CloseRequested prevent_close + hide，退出仅托盘菜单）
- **窗口状态记忆**：onMoved/onResized 防 600ms 抖动存 localStorage（最大化只记状态不覆盖常规尺寸），启动恢复
- **单实例锁**：tauri-plugin-single-instance 首位挂载，二次启动唤窗置前
- **验证**：CDP 桌面窗口——标题栏+dragRegion+3 按钮 ✅ / desktop-glass + body 76% 透明 ✅ / set_window_effect OK ✅ / 0 报错 ✅；无头 Chrome Web 回归——无标题栏/无玻璃类/不透明白底 ✅
- **0.2.0 安装包**：`Onda Player_0.2.0_x64-setup.exe`（release 重编译 + NSIS）

## 遇到的问题
- capabilities 写了不存在的 `core:window:allow-on-*`（窗口事件监听权限并入 core:event:allow-listen）→ build script ACL 校验失败；删掉即过
- release 链接报「拒绝访问 os error 5」→ 残留 app.exe 进程占用，taskkill 后过（tauri dev 的实例也要算在内）

## 明日待办
- 9.6 真机验收：托盘左键/菜单/关窗隐藏/退出、单实例唤起、磨砂深浅观感、拖拽与双击最大化
- 全量双形态回归 + git tag v0.2.1

---

# 会话 9：修「无法拖拽窗口」+ 窗口状态记忆屏幕外坐标 bug【第 9 阶段】

## 今日完成
- **拖拽修复**：用户反馈 0.2.0 无法拖动窗口——根因是 capabilities 漏了 `core:window:allow-start-dragging`（不在 core:window:default 默认集，data-tauri-drag-region 底层调 start_dragging 会被拒）。补权限后直调 `start_dragging` 验证：**进入系统模态拖拽循环（3 秒不返回、无权限报错）**，链路已通。CDP 合成鼠标驱动不了 OS 级拖拽循环，故以直调结果 + 用户真机确认为准
- **窗口状态记忆屏幕外坐标 bug**（拖拽排查中顺手发现）：最小化时 outerPosition 读到 (-32000,-32000,276×45) 且被存入 localStorage，之后每次启动都被"恢复"到屏幕外。修复：保存前 isMinimized/isMaximized 跳过；恢复前校验合理性（w≥600/h≥400/坐标有界），脏数据 center() 兜底
- capabilities 补 start-dragging / unminimize / show / set-focus / unmaximize / is-minimized
- 版本 0.2.1 重打安装包

## 遇到的问题
- dev 实例被单实例锁挡退：已安装版运行中时共享同一 identifier，dev 第二实例直接秒退（exit 0，现象迷惑）；先结束已装实例再开发
- CDP Input 合成拖拽对模态拖拽循环无效（合成事件在 Chromium 层，Win32 循环收不到）——桌面交互只能真机验证
- 直调 start_dragging 可能留下未收尾的模态循环，窗口一度最小化怪态；重启实例恢复

## 明日待办
- 用户 0.2.1 真机验收：拖拽/双击最大化、托盘、磨砂观感、单实例
- 通过后 git tag v0.2.1 收官第 9 阶段
