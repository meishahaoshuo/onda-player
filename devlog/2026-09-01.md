# 2026-09-01 开发日志

## 会话 19（项目交接盘点）

### 背景
新会话接手项目，未改动任何业务代码，仅做交接盘点与状态核验。

### 盘点结论

**代码状态**
- HEAD = `97f6d2c`（2026-08-30 22:30），工作区干净（`git status` 无输出），无未提交改动
- 共 20 个提交：阶段 0–7 各一个，之后 12 个为收尾后的 UI 打磨提交
- `dist/` 产物时间 08-30 22:29，与最后一次源码改动（LyricsFullView.vue 22:29）同步 → **dist 为最新，无需重新构建**

**验证结果**
- `npm run build`：`vue-tsc -b` 类型检查**通过**（无类型错误），随后 `vite build` 在清空 `dist/assets` 时失败
- 失败原因：WorkBuddy 沙箱的 safe-delete 走回收站失败（`Error during a 'trash' operation: Some operations were aborted`），属**环境问题，非代码问题**
- 结论：开发端口 5180、生产启动器端口 5181 均未被占用；用户侧双击 `启动播放器.vbs` 不受影响

**文档与代码已脱节（本次最重要的发现）**
- `docs/03-设计规范.md` 仍描述**旧版冷色设计**（`--accent: #4084f4`、深蓝黑 `rgba(24,27,33,.72)`），而 `src/styles/main.css` 已换成 **Aria·咏叹 暖色设计系统 v2**（`--accent: #e8956b`、暖炭基底 `#16120f`、液态玻璃令牌 `--glass-*`）
  - ⚠️ AGENTS.md 要求「写任何 UI 前后必读 03」，按现文档写会写出与全站相反的配色
- `docs/02-技术方案.md` 目录结构缺项：实际已有 `services/palette.ts`、`stores/ui.ts`、`stores/playlist.ts`、`components/ProgressSlider.vue`、`components/AppIcon.vue`、`services/testBridge.ts`
- `docs/04-开发步骤.md` 停在「全部阶段完成」，未记录阶段后的 12 个提交（液态玻璃重构、Logo、更名、歌词页取色渐变/布局重做/动画修复等）

**日志组织**
- `devlog/2026-08-29.md` 实际记录了会话 10–18（跨天续写），`devlog/2026-08-30.md` 只记了双击启动器 → 两份日志日期与内容错位，后续接手易看漏

### 待办（按优先级）
1. 【高】重写 `docs/03-设计规范.md`，对齐 Aria 暖色 + 液态玻璃令牌（否则后续 AI 会话会写错配色）
2. 【中】补齐 `docs/02-技术方案.md` 目录结构与 store 说明；在 `docs/04` 追加「阶段后打磨」小节
3. 【中】整理 devlog 文件名与会话编号错位问题
4. 【低】待用户实测项：真实文件夹选择器流程、MediaSession 媒体浮窗、万首级歌库性能

### 补充说明
- 沙箱内执行 `vite build` 会因清空 outDir 触发 safe-delete 失败；如需在会话内构建，先确认是否需要保留现有 `dist`（它是 `启动播放器.vbs` 的实际服务目录，不建议删除）

---

## 会话 20（两个体验问题修复：歌词页转场 + 封面画质）

### 用户反馈
1. 歌词页飞入动画收尾有明显的"顿一下"——怀疑是缓动曲线或主线程阻塞
2. 歌词页封面太糊，看起来像低分辨率缓存图，怀疑没加载正确尺寸的源

### 根因分析

**封面画质**：`src/services/scanner.ts` 的 `makeThumb` 把封面缩到**最长边 256px** 的 JPEG 写入 IndexedDB 供全站使用；歌词页 CSS 把封面渲染为 `min(42vh, 30vw)`（1080p 屏约 453px CSS，DPR2 屏要 906 设备像素）—— 256px 源直接糊。

**动画收尾顿挫**：`flyIn` 用 WAAPI 同时动画 `left/top/width/height`——这些是**布局属性**，每帧都触发布局重算；页面上还有 `blur(70px) scale(1.35)` 的全屏背景和 `backdrop-filter: blur(46px)` 的迷你条，布局变化会强制让这些大模糊层重新栅格化，收尾时尤其明显。叠加三个并发问题：
- 落地瞬间 `.cover-col` 的 300ms 切歌淡入还在进行（opacity ≈ 0.87→1）→ 视觉上像闪一下
- 飞行期间歌词列表的 mask + 逐行 blur 在做 700ms 的 smoothScroll
- 全屏 blur 背景的 520ms 淡入由 immediate watch 在飞行前就触发了，rasterization 卡在飞行开始附近

### 修复

**封面（按需高清提取 + 切歌预热）**
- 新增 `src/services/cover.ts`：`extractHiResCover(song)` 从音频内嵌图直接 parse（`duration: false, skipPostHeaders: true`，只解析头部），最长边缩到 1024px JPEG q0.92；空封面/失败返回 null
- `library` store 新增 `coverUrlHi(coverId)`：内存 LRU（上限 32，超出回收最早的 objectURL），同 coverId 并发去重
- `CoverImage` 加 `hires` prop：先挂 256px 缩略图立即有画面，再静默换高清图（`img.decode()` 完成后再替换 src，替换瞬间即可见，无二次解码闪烁）
- `PlayerBar` 监听 `player.current.coverId`，用 `requestIdleCallback` 预热当前曲目的高清图——打开歌词页秒出，flyIn 不再等它
- 专辑详情头部封面同步加 `hires`（192px 在 DPR2 屏同样偏软）

**动画（transform-only FLIP + 全程不抢主线程）**
- `flyIn` 改为**只动 transform** 的 FLIP：克隆**目标**封面（不是播放条小图，确保落地时是同一张图，零内容/清晰度跳变），按目标尺寸铺在终点，用 `transform: translate + scale` 反向缩回起点，再动回单位矩阵。Easing 从 `cubic-bezier(0.22, 1, 0.36, 1)`（easeOutQuint，尾段近乎静止）换成 `cubic-bezier(0.32, 0.72, 0, 1)`（Apple sheet 标准减速曲线，收尾干脆）
- 飞行期间 `.cover-col` 不参与切歌淡入淡出（新增 `.main.fly-active .cover-col { opacity:1; transition:none }`），避免落地瞬间还在半透明
- 飞行期间跳过歌词 smoothScroll（落地后用 `scrollToActive(false)` 一次性直接定位，无 700ms 过渡）
- 背景渐变：飞行期间不挂 DOM（`v-if` 的 `bgUrl` 在飞行中保持 null），落地后才插入并淡入——把全屏 blur 的栅格化挪到转场之后
- `bg-grad` 的 `blur(70px) scale(1.35)` 降到 `blur(40px) scale(1.15)`：32px 颜色块拉伸后 40px 已足够柔，rasterization 成本约 1/3

### 验证（无头 Chrome + CDP）

测试桥新增 `addCoverSong()`：ID3v2.3 APIC 帧内嵌 1200×1200 细密条纹+文字图（缩略后一眼能看出糊）+ 80 帧静音 MPEG，验证脚本用 `setMode('one')` 锁住单曲循环避免短音频自动切到下一首。

实测（1440×900，headless **软件栅格化**，比真实浏览器更慢）：
- **封面**：`.cover-main img` `naturalWidth=1024, naturalHeight=1024`，`src` 是 blob（命中 `coverUrlHi` 路径），`complete=true`，`lyric-lines=3`（全部 LRC 组都渲染了）
- **动画**：
  - `p50: 5ms, p95: 5.1ms`——95% 帧都在 16ms 以内
  - 飞行结束附近（点击后 30~60 帧）**无任何 >32ms 的帧**，没有"收尾顿一下"
  - 仅有的 4 个长帧：飞行开始前 95ms（页面首次布局）、飞行中段 35ms（歌词布局 settle）、落地后 55/75ms（背景 blur 层 rasterization 触发）
  - `residualClones: 0`，`coverMainOpacity: "1"`，无 console 错误、无运行时异常
- 视觉：截图里 "ARIA 1200" 和同心圆条纹清晰可读（之前是糊的色块）

### 顺带
- 沙箱内 `vite build` 的 safe-delete 限制仍在，用 `--emptyOutDir false` 兜底（dist 里的过期文件是死链接，不影响）
- `.gitignore` 加 `.workbuddy/`（本机项目记忆不污染仓库）

---

## 会话 21（重写 docs/03-设计规范.md，对齐实际设计系统）

### 背景
交接盘点第 1 项：AGENTS.md 要求「写任何 UI 前后必读 03」，但 03 仍描述已废弃的冷色设计（`--accent: #4084f4`），照它写会产出与全站相反的配色。本次只动文档，不改代码。

### 做法
先读全 `src/styles/main.css`（274 行，令牌唯一真源），再逐个组件核对尺寸/字号/状态，才落笔。

### 核对中发现旧文档的错误（都已修正）
| 旧文档 | 实际 |
|---|---|
| `--accent: #4084f4` 冷蓝 | `#e8956b` 暖琥珀（浅色 `#d9744a`） |
| 全篇无液态玻璃令牌 | `--glass-bg/border/highlight/blur` 四件套 + `.glass` 类 |
| 磨砂 `blur(20px) saturate(1.2)` | `blur(32px) saturate(1.7)` |
| 侧边栏 224px / 播放条 72px / 行 56px | 232px / **80px** / 56px（行高对） |
| 歌词封面 `min(42vh, 42vw)` | `min(42vh, 30vw)` |
| 页面标题 22px | 24px / 600 |
| 主题存 IndexedDB `kv` | 存 **localStorage** `settings.themeMode`，且是 dark/light/**system** 三态 |
| 列表表头 12px「次要文字」 | 12px `--text-tertiary` |
| 无 | 缺：封面双档体系、动画性能红线、深浅主题新增变量必须成对定义 |

### 新文档结构（10 节）
0 铁律（禁硬编码 / 令牌优先 / 动效用令牌）· 1 配色双表 · 2 液态玻璃（含"玻璃内禁铺不透明底"约束）· 3 动效令牌 + 内置过渡类 · 4 布局尺寸 · 5 字体层级表 · 6 音质徽标判定顺序 · 7 组件状态 + 已知例外 · 8 封面双档（256/1024）· 9 **动画性能红线**（把会话 20 的 5 个坑固化成对照表 + FLIP 要点 + 验证方法）· 10 主题切换。

### 两处刻意的取舍
- §7.2 明确歌词页纯白文字是**唯一允许的颜色硬编码**——文字压在封面提取的渐变上，底色不可预知。写清楚可以阻止后续会话"顺手改成变量"反而改坏。
- §9 把转场 5 个坑写成 ⛔/✅ 对照表而不只是叙述，因为这类错误复发率高，表格式约束比段落更容易在写动画前被扫到。

### 未做
- `docs/02-技术方案.md` 目录结构补项、`docs/04-开发步骤.md` 追加「阶段后打磨」小节（交接清单第 2 项）
- devlog 文件名与会话编号错位整理（第 3 项）

---

## 会话 22：歌词页视觉升级方向 —— 已否决，未改代码

### 起因
用户贴了三段生图式视觉描述：① 封面取色的沉浸式氛围背景（teal + 暖橙光泄漏、blur(40px)、中心到四角无缝衰减）② 左右分栏歌词页（封面带镜面倒影 + 玻璃底栏）③ 日中双语歌词聚焦排版（当前行纯白加粗放大、非当前行 50% 灰）。

### 差距分析
对照 `LyricsFullView.vue` 逐条核实后发现**多数已实现**，不需要重做：

| 描述要求 | 现有实现 |
|---|---|
| 动态取色背景 + 重模糊 | `palette.ts` 把封面降采样成 32px 色块图，CSS `blur(40px) scale(1.15)` 拉伸。背景色天然跟随封面 |
| teal + 暖橙 | 取决于封面本身，**不是**设计系统改色；强调色仍为暖琥珀 `#e8956b` |
| 左右分栏 | `.main` 已是封面在左、歌词在右 |
| 当前行纯白加粗放大 | 30px / 700 / `#fff` + `scale(1.02)` |
| 非当前行 50% 灰 | `dim-1` 即 `opacity: .5`，并逐级叠加 blur 做景深 |
| 双语歌词 | `g.texts[j > 0]` 渲染为 `sub` 译文行 |
| 玻璃底栏 | `.mini-bar` 已 `backdrop-filter: blur(46px)` |

**真正缺的只有 4 项**：封面镜像倒影、光泄漏辉光、中心→四角径向衰减（暗角）、更强的 karaoke 聚焦。

### 处理过程
按用户选择"先出参考图确认风格再改代码"，生成 3 张概念图（约 15–30 积分）供确认。

### 结论
**用户看完后否决（"放弃这个方案"）。** 未改动任何代码，`LyricsFullView.vue` 保持会话 20 后的状态。

### 遗留
- 3 张参考图已被 `present_files` 收进应用 blob 存储（`.workbuddy/blobs`），源目录 `design-refs/` 因此变空并已删除
---

## 会话 23：Logo 品牌标识落地到网页

### 用户反馈
设计完成几何 J 形 Logo 后，用户要求把新 Logo 正式替换到网页上。

### 改动
1. `public/favicon.svg` → 用新主图标（几何 J 形，透明底，暖琥珀渐变）覆盖旧 favicon。
2. `public/logo.svg` → 删除旧文件，避免与新 `public/logo/` 目录混淆。
3. `src/components/Sidebar.vue` → 侧边栏品牌区图标从 `/logo.svg` 改为 `/logo/aria-logo-app.svg`（白底圆角 App 图标风格，与 `.brand-logo` 的 36px + 圆角 + 阴影样式匹配）。
4. `src/views/SettingsView.vue` →「关于」卡片新增品牌行：App 图标 + 名称 + 版本号 `v0.1.0 · 网页版本地音乐播放器`，并补全 CSS。
5. `index.html` → 新增 `apple-touch-icon`（指向 `aria-logo-app.png`）和 `theme-color: #e8956b`。
6. `npm run build` → 类型检查通过，`vite build` 成功，产物中 `dist/logo/` 完整、`dist/favicon.svg` 为新图标。

### 验证
- 用 headless Chrome + CDP 访问 `localhost:5180`：
  - 侧边栏品牌图标 src 正确，加载成功，渲染 36×36。
  - 设置页关于区 logo、名称、版本号均渲染正常。
  - 无 console error / 无运行时异常。
- 视觉截图确认：浅色主题下白底圆角图标与侧边栏风格协调，设置页品牌行清晰。

### 遗留
- 深色主题下白底圆角 App 图标在玻璃侧边栏上仍为正常 App 图标风格（类似 Apple Music 深色模式），无明显问题。

---

## 会话 23 收尾：深色主题 Logo 适配

### 改动
- `Sidebar.vue` 品牌图标按 `settings.resolvedTheme` 自动切换：浅色用白底圆角 `aria-logo-app.svg`，深色用透明底暖色 `aria-logo-main.svg`。
- 深色模式下给 `.brand-logo` 加 `no-shadow` 类，移除 `box-shadow`，避免透明底图标投出矩形阴影框。
- `SettingsView.vue`「关于」品牌图标同步同样逻辑。

### 验证
- 用 headless Chrome + CDP 实测浅色 → 深色 → 浅色切换：
  - 浅色主题：`src = /logo/aria-logo-app.svg`，`box-shadow` 正常。
  - 深色主题：切 1 次即进入 dark，`src = /logo/aria-logo-main.svg`，`box-shadow: none`。
  - 无 console error / 无运行时异常。
- 视觉截图确认：深色下暖色 J 形浮在玻璃背景上，无白底方块，协调。
