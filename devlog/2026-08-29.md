# 2026-08-29 开发日志

## 今日完成
- 确定技术方案：网页版（Vue 3 + Vite + TypeScript），文件夹授权读取本地音乐【第 0 阶段】
- 手写脚手架（中文目录名导致 create-vite 交互不可用）：package.json / vite.config.ts / tsconfig / index.html / 最小占位应用【第 0 阶段】
- 依赖安装完成：vue、pinia、idb、music-metadata，0 漏洞【第 0 阶段】
- 建立 docs/ 四个标准文件（需求说明/技术方案/设计规范/开发步骤）、devlog/ 模板、AGENTS.md 与 CLAUDE.md【第 0 阶段】
- `npm run dev` 验证通过（占位页正常显示）【第 0 阶段】

## 遇到的问题
- `npm create vite` 因目录名含中文触发交互式询问，非交互环境下被取消 → 改为手写等价脚手架文件，已解决

## 明日待办
- 第 2 阶段：文件夹授权 + 扫描 + IndexedDB 缓存 + 歌曲列表页

---

## 会话 2

### 今日完成
- 第 1 阶段完成【第 1 阶段】：
  - 深浅色 CSS 变量体系（dark/light 两套 + 跟随系统 matchMedia 监听 + localStorage 持久化，`stores/settings.ts`）
  - 内置图标集 `AppIcon.vue` + `icons.ts`（零外部依赖）
  - FrostedPanel 磨砂组件、Sidebar、PlayerBar 骨架、App 布局壳（Pinia ui store 切换视图）
- 浏览器验证：浅色（跟随系统）→ 深色切换即时生效；`data-theme=dark` + localStorage 写入正常；刷新后主题保持
- `npm run build`（vue-tsc 类型检查 + vite 构建）通过
- 开发端口因 5173/5174 被其他项目占用，固定为 5180（strictPort），已更新 AGENTS.md 与 vite.config.ts

### 遇到的问题
- AppIcon 内 `keyof typeof ICONS` 与 props 循环引用导致 TS2502/TS2456 → 图标表拆分到独立模块 `icons.ts` 解决
- `@` 别名只配了 tsconfig 没配 vite → vite.config.ts 增加 resolve.alias；`node:url` 类型缺失 → 安装 @types/node
- 本机 5173 端口被另一个项目（魔方世界）占用，浏览器打开 5173 看到的是别的应用 → 固定 5180 端口解决

### 明日待办
- 第 2 阶段：文件夹授权与句柄持久化、递归扫描、music-metadata 标签解析、封面缓存、歌曲列表页

---

## 会话 3

### 今日完成
- 第 2 阶段完成【第 2 阶段】：
  - 服务层：`db.ts`（IndexedDB 五个 store）、`fs.ts`（授权/句柄持久化/递归枚举）、`scanner.ts`（增量扫描 path+size+mtime、封面 OffscreenCanvas 缩略 256px、分批让出主线程、可取消）
  - `stores/library.ts`：根目录权限状态机（prompt→点击恢复）、扫描进度、封面 ObjectURL 缓存
  - 组件：SongList（虚拟滚动 VirtualList）、QualityBadge（Hi-Res/无损/有损规则）、CoverImage
  - 视图：SongsView（空状态/权限恢复横幅/扫描进度条/列表）、FoldersView（根目录管理）
  - dev-only 测试桥 `testBridge.ts`（生产构建不含）：OPFS 生成测试音频走完整扫描链路
- 浏览器验证：扫描 3 首入库 → 刷新恢复 → 重扫 skipped=3（增量生效）；列表徽标/时长/列布局正常
- `npm run build` 通过（music-metadata 按需分包正常）

### 遇到的问题
- TS lib.dom 未覆盖 File System Access API 的 entries/queryPermission → `types/fsa.d.ts` 补声明
- VirtualList 插槽 item 类型 unknown → `<script setup generic="T">` 泛型组件解决
- Vite 文件监听不可靠：编辑后 served 内容陈旧（HMR 无效），需重启 dev server 才生效（本环境怪癖）
- 浏览器自动化中 playwright.click 对本页按钮超时（原因未明），改用页内 `element.click()` 验证；截图偶发 capture failed，重试即可
- pickFolder 职责重构：只负责选择，持久化统一由 library.registerRoot 承担

### 明日待办
- 第 3 阶段：播放内核（audio 单例/队列/四种模式/进度/音量）、MediaSession、播放状态持久化与恢复

---

## 会话 4

### 今日完成
- 第 3 阶段完成【第 3 阶段】：
  - `services/player.ts`：audio 单例、对象 URL 生命周期、MediaSession 元数据/动作/位置上报
  - `stores/player.ts`：队列、四种模式（顺序播完即停/列表循环/单曲循环/随机洗牌）、进度/音量、文件丢失自动跳下一首、防死循环
  - 状态持久化：防抖写 kv（队列/索引/进度/音量/模式）+ pagehide 立即存；启动恢复为暂停态，点击播放从上次进度续播
  - PlayerBar 接真实数据（进度条点击 seek、音量、模式循环切换）；歌曲列表单击播放 + 播放中高亮/勾选
- 浏览器验证：播放计时推进、next/prev、暂停、seek、shuffle 模式、kv 存档内容正确；刷新恢复 + 续播进度生效（修复了 seek 在 metadata 加载前被重置的 bug）；播放行高亮 1 行 + 勾选图标
- `npm run build` 通过

### 遇到的问题
- 续播进度丢失：恢复场景下 pendingSeek 在 audio metadata 加载前设置会被重置 → 改为监听 loadedmetadata 后再应用，已修复并回归验证
- store 内 watch 数组里放函数源（queueLengthWorkaround）可行但可读性差，后续可重构为直接 watch([queue, index])

### 明日待办
- 第 4 阶段：专辑/专辑详情/艺术家/文件夹/曲风四个聚合视图 + 专辑详情页

---

## 会话 5

### 今日完成
- 第 4 阶段完成【第 4 阶段】：
  - library store 增加专辑/艺术家/曲风聚合 computed（专辑按 碟片号+音轨号 排序）
  - ui store 增加 detailKey 钻取导航（视图内进详情/返回）
  - AlbumsView（封面网格）→ AlbumDetailView（对照截图 3：192px 封面头部、歌曲数/时长/年份统计、列表循环/随机按钮、碟片分组曲目、点曲目播放联动）
  - ArtistsView（圆形头像卡片→详情 SongList）、GenresView（列表→详情 SongList）
  - FoldersView 升级：根目录可点击过滤浏览其下歌曲（defineModel 双向绑定选中项）
- 浏览器验证：四个页面进出与数据均正常，详情页点曲目播放条联动正确
- 修复 formatTotalDuration 不足 1 分钟显示"0 分钟"的问题

### 遇到的问题
- 内嵌自动化浏览器重启会清掉临时 profile 的 IndexedDB（真实 Chrome/Edge 持久化到磁盘，不受影响）→ 测试数据重灌即可
- DOM 里 .song-row 计数翻倍是 v-show 隐藏的 SongsView 仍挂载所致，非 bug

### 明日待办
- 第 5 阶段：歌单（新建/重命名/删除/添加歌曲/排序/歌单播放）

---

## 会话 6

### 今日完成
- 第 5 阶段完成【第 5 阶段】：
  - `stores/playlist.ts`：歌单 CRUD + addSongs/moveSong/removeSong，全部即时持久化
  - PlaylistsView：歌单网格 → 详情（播放全部/随机/添加歌曲弹层带搜索/重命名/删除/拖拽排序/单首移除），磨砂弹层（FrostedPanel + teleport）
  - 侧边栏：显示歌单列表、点击直达歌单详情；「新建歌单」直达创建弹窗（ui store 增加 requestPlaylistCreate 标记）
- 浏览器验证：创建→加歌→播放联动→刷新不丢；排序/重命名跨刷新保留
- **重要修复**：rename 持久化静默失败 —— 根因是 `{...p, name}` 浅拷贝把 Vue 响应式 Proxy 的 songPaths 数组传入 `IDBObjectStore.put`，抛 `DataCloneError`（void 吞掉了 unhandled rejection）。修复：put 前显式还原普通数组 `[...next.songPaths]`
- 修复 UX 断层：侧边栏「新建歌单」原先只跳页不弹窗

### 遇到的问题
- Vite 文件监听在本环境多次滞后（served 内容陈旧）→ 凡遇到"代码明明改了但行为没变"，先重启 dev server
- 排查 DataCloneError 的方法：window 挂 unhandledrejection 监听捕获（void 掉的 Promise 也能抓到）

### 明日待办
- 第 6 阶段：全屏歌词页（.lrc 解析/时间戳分组/模糊背景/逐行高亮滚动/点击跳转）
