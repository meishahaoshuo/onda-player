# 04 开发步骤

> 开发前必读本文件确认当前阶段；完成一个阶段才算下一个。进度状态：⬜未开始 / 🔄进行中 / ✅已完成

## 第 0 阶段：项目初始化 ✅
- [x] git init + .gitignore
- [x] Vite + Vue3 + TS 脚手架（手写，因中文目录名交互限制）
- [x] 依赖安装：vue / pinia / idb / music-metadata
- [x] docs/ 四个标准文件、devlog/、AGENTS.md、CLAUDE.md
- [x] 最小占位应用可运行
- **验收**：`npm run dev` 打开 localhost:5173 显示占位页 → 已验证

## 第 1 阶段：主题体系 + 布局骨架 ✅
- [x] 深浅色 CSS 变量体系（03-设计规范 全部变量），跟随系统/手动切换/持久化
- [x] FrostedPanel 磨砂组件
- [x] App 布局壳：侧边栏（导航项占位）+ 底部播放条（控件占位）+ 内容区
- **验收**：切换主题即时生效且刷新后保持；磨砂面板效果符合 03-设计规范 → 浏览器验证通过（深色切换 + localStorage 持久化 + 刷新保持）；开发端口固定为 5180（5173/5174 被其他项目占用）

## 第 2 阶段：文件夹接入 + 音乐库 ✅
- [x] 文件夹选择与句柄持久化（多文件夹）、重开授权恢复
- [x] 递归扫描 + music-metadata 解析 + 封面缩略图缓存
- [x] 增量扫描（path+size+mtime）、进度显示、可取消
- [x] 歌曲列表页（徽标/高亮/时长，虚拟滚动）
- **验收**：通过 dev-only 测试桥（OPFS 测试音频）验证：扫描 3 首入库 → 刷新后从 IndexedDB 恢复 → 重扫全部跳过（增量生效）；列表徽标/时长/表头正常；真实系统文件夹选择器路径留待用户实测

## 第 3 阶段：播放内核 ✅
- [x] 播放/暂停/上下曲/进度/音量/四种播放模式
- [x] 播放中高亮 + MediaSession
- [x] 队列/进度/音量/模式持久化与恢复
- **验收**：测试桥验证：播放→计时推进、上下曲、暂停、模式切换、进度 seek、kv 存档；刷新恢复为暂停态且点击播放从上次进度续播（1.05s→1.28s）；播放行高亮+勾选图标正常。MediaSession 媒体浮窗需真实桌面验证（自动化环境无法确认）

## 第 4 阶段：专辑/艺术家/文件夹/曲风页 ✅
- [x] 四个聚合视图 + 专辑详情页（大封面头部/统计/播放与随机/碟片分组）
- **验收**：浏览器验证：专辑卡片→详情页（标题/统计/3 曲目/返回按钮）；详情页点曲目联动播放条；艺术家卡片→详情 SongList；曲风列表→详情；文件夹页支持选中根目录过滤浏览。聚合数据不足 1 分钟显示"秒"已修正

## 第 5 阶段：歌单 ✅
- [x] 新建/重命名/删除/添加歌曲/拖拽排序/歌单播放
- **验收**：浏览器验证：侧边栏「新建歌单」直达创建弹窗 → 创建「我的最爱」→ 添加 2 首 → 播放联动；排序 moveSong、重命名、刷新后歌单与顺序均保留（修复 rename 的 DataCloneError 持久化 bug）；侧边栏显示歌单项

## 第 6 阶段：全屏歌词页 ✅
- [x] .lrc 解析 + 时间戳分组（翻译/罗马音）
- [x] 模糊封面背景/封面倒影/逐行高亮滚动/点击跳转/深浅色适配
- **验收**：浏览器验证：双语 .lrc 解析为 2 组 4 行（时间戳 30ms 差合并成功）；当前行 28px 白色放大、其余按距离递减透明度；封面倒影渲染；页面有不透明底色不透穿（修复无封面时整页透明问题）；无歌词显示占位文案

## 第 7 阶段：设置页 + 收尾 ✅
- [x] 文件夹管理/主题设置
- [x] `npm run build` 产物 preview 验证、整体回归、最终提交
- **验收**：设置页三卡片（外观三模式切换即时生效 / 音乐文件夹管理带扫描进度 / 关于说明）；生产构建 preview 正常渲染且不含 dev 测试桥；整体 DOM 回归通过

---

## 项目状态：✅ 全部阶段完成

### 阶段后收尾与打磨
- [x] 歌词页转场 + 高清封面链路修复（会话 20）
- [x] 设计规范文档对齐暖色设计系统 v2（会话 21）
- [x] 歌词页视觉升级方向被否决，未改代码（会话 22）
- [x] 几何 J 形 Logo 设计定稿并导出（会话 22 与本会话）
- [x] Logo 替换到网页：favicon / 侧边栏品牌区 / 设置页「关于」/ apple-touch-icon / theme-color（会话 23）
- [x] 深色主题下 Logo 自动切换为透明底主图标，避免白底圆角图标在深色玻璃背景上过跳（会话 23 收尾）
- [x] 新增 PWA manifest.webmanifest，补全移动端安装元信息（会话 23 最终）
- [x] 全站换色到 Apple Music 红白风：main.css 令牌重写、5 视图+PlayerBar+LyricsFullView 令牌化、PWA 主题色与 manifest 对齐、aria-logo-app.png 重渲染中性化阴影、docs/03 完全重写（**会话 24**）
- [x] 修复歌曲列表表头列与行列错位：`.list-header` 与 `.song-row` 共用 5 列 grid，表头漏 `col-cover` 占位致整体左错一格；补空占位后 CDP 实测 5 列 Δx 全为 0（**会话 25**）
- [x] 专辑跳转过渡重做为「引力坍缩」：周边卡片按距离吸入点击点 + 详情页从被点卡片矩形析出放大 + 封面 FLIP 飞行 + 内容距离波前浮现，返回全对称反向；详情层移出 `<Transition>` 改为 `.content` 覆盖层（网格常驻保滚动位置）；新增封面主色预取 `paletteCache`（环境光晕开场即用，`.header-ambient` 去实时 blur 改预烘焙 PNG）（**会话 26**）
- [x] 品牌重塑 Aria·咏叹 → **ONDA·澜**：品牌书 `brand/minimal` 五方向定稿后全套落地——标题/manifest/Sidebar/关于页改名，favicon 与 logo 资产换海军蓝三层渐变声波，main.css 主色令牌切海军蓝（浅 `#172554` / 深 `#5c7ce0`），theme-color 同步；`--danger` 语义红保留（**会话 36**）
- [x] 歌词卡拉OK改 Apple Music 式逐字点亮：解析层捕获增强型 LRC 逐字时间（`charTimes`，词内均分）；active 行按码点拆字符 span，rAF 直写 DOM 逐字填充；无逐字数据时按字符类型加权虚拟插值兜底；字符级三停渐变柔边；歌词偏移同步平移逐字时间；顺手修 `syncLyricRaf` 未归零导致暂停后兜底 watcher 永久短路的存量 bug（**会话 37**）
- [x] 修复歌词页飞入过渡结尾封面顿挫：根因是鼠标视差 `--mx/--my` 在飞行中被更新（克隆挂 body 吃不到页面级变量，与真实封面错位）+ 克隆圆角/投影写死与真实不符；修法为飞行/关闭期间冻结视差（JS 守卫 + `.fly-active .cover-main { translate: none }` 双保险）、克隆样式改读计算样式；逐帧采样实测交接 Δ=0、飞行中封面漂移 0（**会话 38**）

- [x] 专辑详情头部信息面板：右侧磨砂玻璃材质（复用 .glass）展示流派/音质徽标/格式/总大小/碟片/平均码率，数据全部派生自扫描字段，缺数据自动隐藏；弹出式入场动效（**会话 38/41**）
- [x] 功能扩展八项（**会话 42**）：①去曲风板块，新增排行榜（听满 30s 或 50% 进度计 1 次，Top 50 金银铜名次徽标，可展开全部，容量可设置）②我喜欢的音乐板块（播放条爱心 + 右键/悬停收藏，IndexedDB 持久化）③设置页重构为双列卡片并新增播放行为（恢复队列/自动续播开关）、歌词（字号/偏移）、排行榜选项、数据管理（清空统计/收藏/全部资料，确认弹窗）④新建歌单/重命名/添加歌曲弹窗精修，添加歌曲改多选批量 ⑤歌单 2×2 拼贴封面（≥4 张封面拼贴，否则单曲封面/占位）⑥歌曲页顶部管理按钮移除，统一收敛到文件夹板块 ⑦播放队列面板支持拖拽排序/单曲删除/清空/自动定位当前行 ⑧全局歌曲右键菜单（播放/下一首播放/收藏/添加到歌单含新建并加入/查看专辑/查看艺术家/从歌单或队列或资料库移除）+ 列表悬停快捷操作；DB v2 新增 stats/favorites 存储
- [x] 体验优化九项（**会话 43**）：①进度条时间气泡在详情页不被遮挡（player-bar z-index 6）②侧栏去除主题切换按钮、新建歌单并入歌单板块（标题行悬浮 + 按钮）③歌单可手动设置封面（PlaylistRecord.coverPath + 详情页选择弹窗按封面去重 + 侧栏/列表/详情同步显示）④歌曲行悬停按钮重做为玻璃胶囊，浮现在专辑与时长间空白区，不再遮挡时长（SongList/专辑详情/队列/歌单四处）⑤排行榜前三名改领奖台形式（金银铜台柱+角标徽章+错峰升起），移除榜单容量设置⑥点击歌曲时行内封面弧线飞入播放栏 + 落点弹跳（coverFlight 服务，与页面过渡编排互斥）⑦列表条目滚动错峰浮现（useStaggerReveal：专辑/艺术家网格、排行榜、歌单网格、SongList 首屏窗口期）⑧设置页重构为左侧分类导航式（外观/音乐文件夹/关于），删除播放行为/歌词/排行榜容量/数据管理 UI（store 字段保留）
- [x] 功能与体验十四项（**会话 44**）：①排行榜清空按钮移入设置「数据」分类 ②顶栏搜索（结果列表式，标题/艺术家/专辑过滤）③文件夹板块双栏重构（玻璃卡+权限点+悬停操作+扫描进度条）④歌单封面过渡接入引力坍缩编排器（详情改覆盖层、网格常驻、删除 legacyFlip）⑤快捷键系统（默认 Space/Ctrl+方向键/Ctrl+M，设置可录制、冲突提示）⑥侧栏歌单封面 28px+导航视觉打磨 ⑦歌单右键菜单（播放/重命名/设置封面/删除）⑧悬停按钮去胶囊底融入行背景 ⑨歌单拖拽换 useDragReorder（长按+FLIP 让位）⑩添加歌曲弹窗重构（全选/反选/仅看未添加/封面行/已选计数）⑪自定义主题（7 预设色板+取色器+accent 派生 override）+封面氛围光（内容区取色光斑，可开关）⑫最近在听板块（stats.lastPlayed 降序）⑬侧栏拖拽排序（主导航+歌单子项，navOrder/kv 持久化）⑭齿轮设置图标；新基础设施：useDragReorder composable、PlaylistContextMenu、hotkeys 服务
- [x] 搜索框作用域修复（**会话 46**）：新增 `composables/useSearchScope.ts`，顶栏搜索按当前上下文收窄——歌单/专辑/艺术家/我喜欢的音乐/最近在听/排行榜/文件夹，无收窄语义的位置退化为全库；结果顶部显示「在「X」中找到 N 首」+「本范围 / 全部歌曲」切换，空态给一键切全库，placeholder 随作用域变化；顺带修掉搜索态下导航无响应、详情覆盖层遮挡搜索结果、文件夹根目录未入 store 三个关联问题
- [x] 自定义背景壁纸（**会话 46/47 引入，会话 48 彻底移除**）：设置 → 外观的壁纸选择/通透程度滑杆已整体拆除——删 `services/wallpaper.ts`、App.vue 壁纸层与淡入过渡、设置页壁纸 UI、settings store 浓度字段与 `--wallpaper-intensity` 写入、main.css 两主题全部 `--wallpaper-*` 令牌与整个「壁纸模式」区块（详情层透壁纸背景/玻璃令牌加固/控件半透明规则）、ChartsView 关联注释、cover.ts `downscale` 还原为模块私有；App 启动时自动清理 IndexedDB 遗留的 `wallpaper`/`wallpaper-blur` kv 数据（CDP 13 项断言全绿：遗留 kv 被清、无壁纸层/属性/变量、设置页无壁纸文案且氛围光保留、两主题各视图正常）
- [x] 文件夹页空态布局修复（**会话 48**）：根因为 `.primary-btn/.ghost-btn` 共享规则里的 `flex:1`（左栏底部工具栏横向均分用）落到空态纵向 flex 容器后作用到主轴，把「添加文件夹」按钮拉成通屏高竖条；`flex:1` 收进 `.side-toolbar` 作用域根因修复 + 空态图标加玻璃圆角底座（与 root-card 玻璃语言呼应），深浅两主题截图确认、工具栏均分回归通过
- [x] 歌词页「封面色场」改造（**会话 48**，对照 Salt Player）：背景从固定米白改为封面主导——`palette.buildCoverField` 把封面 cover-fit 缩放 + 画布内 `blur(20px) saturate(0.7)` + 白色自适应提亮（暗封面多提亮）预烘焙成 256×(144~256) PNG dataURL（无实时全屏 CSS blur 铁律），`.bg-ambient` opacity 0.16→1 成为整页色场、切歌 260ms 短淡入（无双层）；`pickMainColor` 取高饱和中亮块为主色，`deriveLyricVars` 按 WCAG 对比度循环压暗（当前行/控件 ≥7:1、其余 ≥4.5:1）派生 21 个 `--lyric-*` 动态令牌经根元素内联继承到全部子元素；删流光圆斑；无封面/取色失败回落静态米白；reduced-motion 全跳过（CDP 15 项断言全绿含对比度与兜底）
- [x] 立体歌词景深阶梯（**会话 48**，对照 Salt Player 截图；**同日反馈后整体下线**）：曾实现当前行放大 + 远行缩小/透明递减/模糊陡增的透视纵深与设置开关，用户要求去除——立体景深（开关/缩放阶梯/透明加深/active 放大）已全部拆除，**恢复原版景深模糊**（dim 透明 0.6/0.45/0.32/0.22 + blur 0.4/1/1.8/2.6px，`settings.lyricBlur` 独立开关）；保留滚动 Q 弹（easeOutBack 过冲回弹 220-460ms）
- [x] 添加文件夹「封面漩涡卷入」动画（**会话 49**；黑洞→漩涡历经四轮反馈迭代）：扫描中每 8 首落库一批，对应封面（同专辑去重）**从漩涡四周随机环带沿极坐标螺旋向心汇聚**（统一顺时针与水纹同向、r 平方衰减近核急缩、θ 平方加速绕行、scale 0.92→0.14）；**漩涡本体**悬浮**视口正中心** 176px——俯视排水口样式：深色漏斗口（accent 深色渐变，非纯黑）+ 三条 SVG 阿基米德螺旋水纹（14s 慢转）+ 口沿张力细环 + 固定水面高光，全程无 filter blur 走纯合成层；**在飞计数精确收尾**（inFlight 归零即「水被抽空」式坍缩）；数据流 = scanner `onBatch` 回调 → library `scanBatchSink` 槽位 → `absorbFlight.ts`（App onMounted 注入）；四道闸门 + 双重限流（会话 24 张/批 6 张）+ coverUrl 竞速 300ms；测试桥 `addBulkSongs(n)`（CDP 10 项断言：收尾延迟 0ms/全程无 filter/限流/reduced-motion）
- [x] 主题色预设色板换新（**会话 49**）：旧 7 浊色换现代鲜亮 8 色——靛蓝 #5865f2 / 天青 #0284c7 / 翡翠 #0ca678 / 琥珀 #d97706 / 珊瑚 #ef5350 / 玫红 #e64980 / 紫罗兰 #8b5cf6 / 石墨 #64748b（简单亮度全部 0.42~0.52 → accent-text 统一白字且离阈值 ≥0.10）；「默认海军蓝」与自定义取色器保留，布局无需改（CDP 断言点击生效/回默认）

- [x] 九项反馈改造（**会话 45**）：①修歌词页切歌穿模（换带 watcher 加 lyricsOpen 守卫）②修侧栏点击热区只有文字（button 不自动撑满，nav-item 补 width:100%）+ 悬停/选中强调分层 + 长按吞点击修复（仅真实拖拽位移才抑制 click）③修歌单返回动画结尾（网格去 v-show 常驻 + closing 防重入）④排行榜表头移到列表区上方并与列对齐 ⑤添加歌曲重构 900×640 大号二级弹层 ⑥侧栏歌单封面 28→36px/行高 48px ⑦新 AppSwitch 共享开关统一设置页与弹层动效 ⑧歌单去拖拽改自定义排序（自定义顺序/歌名/艺术家/专辑/播放次数/时长，kv 持久化）⑨歌单详情头部对齐专辑详情页（渐变底+呼吸光斑+噪点+176px 封面+统计行）
- [x] 胶囊播放条进度改「封面色氛围光 + 播放头光尘」（**会话 50**，同日多轮迭代；中途按用户要求试过鸿蒙 7 官网「沉浸光感」的光带/光扫/光晕引力，最终全部拆掉）：进度不再是沿外框的弧线，改为「常态一层封面色氛围光；拖拽时播放头附近浮着一团随机漫游的光尘、整条微微提亮」——氛围光改用**封面取色**（`palette.extractBrightColors` 新增 `lift` 选项保留原始饱和色；`paletteCache` 改为一次采样同产 `base`+`bright`，并新增 `brightColors()`（并发共享同一 Promise、4s 超时兜底））；光尘 **80 颗**（**20 层 × 每层 4 颗**），每层只用**一个 1px 圆点 + 一条 `box-shadow`** 画出整层散点，整团挂在播放头（`.pt-dusts` 用 `--cp-x` 平移、只铺 ±72px）并按各自 4 航点闭合路径随机漫游（±34px/±17px、5~11s 一圈、相位错开、循环无跳变）＋ 一档更快的小幅快抖（`translate`，±2~5px、2.2~4.2s）——两个频率叠加才有流动感，实测同时刻 20 层 transform 全不同；深色＝白色微光、浅色＝封面色微尘（白点在白玻璃上看不见），单颗直径 ≈ 1px + 2×spread + 2×blur ≈ 2px；收边由 `.pt-field` 的 mask 固定 68px 软收尾；**没有光带、没有指针光晕、没有换歌光扫**；**点/拖落在播放条空白处**（`isBlankArea()` 排除按钮/输入/封面/队列面板：点一下即 seek、横移即拖拽）；**拖拽时整条微微发光**（页脚 `::after` 很淡的封面色外辉光 30px/28% + 内侧呼吸 20px/13%）；光标空白处 `grab`、拖拽中 `grabbing`；暂停即静止且弱一档、`prefers-reduced-motion` 关漫游与呼吸、整体受「封面氛围光」开关管辖（CDP 全绿：20 层/80 颗且团心与 `--cp-x` 严丝合缝/同时刻各层 transform 全不同/旧元素已彻底移除/常态光尘与辉光 opacity 0、拖拽中为 1、松手回 0/点空白处真的跳过去/点控件不 seek/白尘取色与换歌换色/mask 停靠点/wash/暂停与降级/标准条无特效层/控制台 0 报错）

- [x] 六项反馈改造（**会话 51**）：①专辑/艺术家/排行榜删 `useStaggerReveal`，切页不再错峰浮现 ②「封面氛围光」开关整体下线改为默认常开（store 字段与 localStorage 遗留清理）③播放队列面板点空白处自动关闭（document 捕获监听；播放条空白只收面板不 seek）④文件夹页选中文件夹后「重新扫描」只扫该文件夹（动画只演该文件夹封面）⑤歌曲行悬停按钮居中于标题内容与艺术家列之间的空白（悬停时 Range 测量标题右端写 `--title-end`）⑥歌单封面弹层去「自动拼贴」、新增「导入图片」自定义封面（`customCoverId` 入 covers 库、与 coverPath 互斥、三处封面解析统一为 `playlistManualCoverId`，弹层补「恢复默认」）

- [x] 顶栏磨砂玻璃化 + 移除列名表头（**会话 52**）：`view-header`（页面标题+搜索框）提出文档流改悬浮磨砂层（`.content` 上 `--head-h:68px`，`view-body` 顶部等高留白，内容滚动从顶栏玻璃底下穿过；材质走 `--glass-bg/--glass-blur` 普通磨砂）；吸附位统一 `--list-sticky-top` 变量（设置页导航吸在顶栏下缘）；SongList 与排行榜的列名表头按需求整体移除，行自身 grid 对齐

剩余由用户实测的事项：
- 真实音乐文件夹选择器流程（自动化环境无法操作系统对话框）
- MediaSession 媒体浮窗在真实桌面的表现
- 大型真实歌库（万首级）的扫描与滚动性能
- 会话 43 真机手感：封面飞入动画、首屏错峰浮现
- 会话 44 真机手感：侧栏/歌单拖拽排序、快捷键实际触发、氛围光深色主题观感、自定义取色器体验
- 会话 45 真机确认：歌词页开着切歌无异物浮层、侧栏点击热区与长按点击、歌单返回动画收尾、排行榜表头对齐观感、添加歌曲大弹层与开关手感、歌单排序与详情页新头部
